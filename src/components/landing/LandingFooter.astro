---
const currentYear = new Date().getFullYear();

const productLinks = [
  { name: "CRM Entegrasyonu", href: "/tr/products/crm" },
  { name: "Stok Yönetimi", href: "/tr/products/stock" },
  { name: "Satış Analizi", href: "/tr/products/sales" },
];

const companyLinks = [
  { name: "Hakkımızda", href: "/tr/about" },
  { name: "İletişim", href: "/tr/contact" },
  { name: "Kariyer", href: "/tr/careers" },
];

const legalLinks = [
  { name: "Gizlilik Politikası", href: "/tr/privacy" },
  { name: "Kullanım Koşulları", href: "/tr/terms" },
  { name: "KVKK", href: "/tr/kvkk" },
];
---

<footer class="w-full border-t border-border/50 bg-background">
  <div class="mx-auto max-w-6xl px-4 py-12 md:py-16">
    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-4">
      <!-- Brand -->
      <div class="flex flex-col gap-4">
        <a href="/" class="flex items-center gap-2">
          <img src="/logo-dark.png" alt="Ayova" class="h-8 w-auto rounded-none" />
        </a>
        <p class="text-sm text-muted-foreground leading-relaxed">
          Yapay zeka destekli iş çözümleri ile şirketinizin dijital dönüşümünü hızlandırın.
        </p>
      </div>

      <!-- Ürün -->
      <div>
        <h3 class="mb-4 text-sm font-semibold text-foreground">Ürün</h3>
        <ul class="flex flex-col gap-3">
          {productLinks.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground transition-colors hover:text-foreground">
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Şirket -->
      <div>
        <h3 class="mb-4 text-sm font-semibold text-foreground">Şirket</h3>
        <ul class="flex flex-col gap-3">
          {companyLinks.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground transition-colors hover:text-foreground">
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Yasal -->
      <div>
        <h3 class="mb-4 text-sm font-semibold text-foreground">Yasal</h3>
        <ul class="flex flex-col gap-3">
          {legalLinks.map((link) => (
            <li>
              <a href={link.href} class="text-sm text-muted-foreground transition-colors hover:text-foreground">
                {link.name}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="mt-12 border-t border-border/50 pt-8">
      <p class="text-center text-sm text-muted-foreground">
        &copy; {currentYear} Ayova Ltd Şti. Tüm hakları saklıdır.
      </p>
    </div>
  </div>
</footer>
